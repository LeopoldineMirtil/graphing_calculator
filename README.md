# Graphing Calculator

The user enters a number (1 to 10) to run the function of their choice and inputs the information when prompted to complete their selection. The functions that can be performed on this calculator include creating a table of values, plotting line and quadratic functions, solving systems of equations, zooming in and out on a graph, solving quadratic equations.
